# Local Streamlit secrets (optional — same keys as .env)
# Copy to .streamlit/secrets.toml for: streamlit run app.py
# Do NOT commit secrets.toml (listed in .gitignore)

TMAP_APP_KEY = ""
DATA_GO_KR_SERVICE_KEY = ""
OPENAI_API_KEY = ""
OPENAI_MODEL = "gpt-4o-mini"
